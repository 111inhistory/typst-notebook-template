#import "lib.typ": notebook-theme

#show: notebook-theme

= 主题演示文档

这份文档用于验证笔记模板在常见技术写作场景下的表现。正文会同时覆盖中文、English、数字 `2026`、链接
、引用、公式、代码、列表、表格、图片占位和脚注等元素，便于一次性观察整体样式。

段落首行缩进、两端对齐、中文与 Latin 混排、*强调文本*、*术语斜体*、以及#underline[下划线文本]都
应当能够正常呈现。同一段里再补一个链接：#link("https://typst.app/docs")[Typst 文档]，以及一个
行内公式 $f(x) = x^2 + 2x + 1$。

== 基础段落

这个小节用于观察常规正文密度与阅读节奏。为了让页面上出现更稳定的排版结果，这里补充几句较长的说明
文字。模板若设置了字重、行距、段距、首行缩进和自动断字，此处通常最容易直接看出差异。

第二段专门测试交叉引用。后文会出现@fig-layout、@tbl-metrics 和@eq-energy，方便确认题注编号、补充
名称和引用样式是否一致。

=== 强调与语义

- `inline code` 应显示为行内代码盒。
- *重点概念* 应保持与正文字体不同的强调效果。
- #strong[强强调文本] 应具备更明显的字重变化。
- 这里再加入一个混合片段：函数`render_note()`会在保存后触发一次局部重绘。

=== 脚注

脚注用于测试正文标记、脚注区分隔以及脚注段落的行距配置。这里放一个脚注入口
#footnote[脚注内容用于确认编号、位置与文本样式。]
，再放一个更长一些的脚注#footnote[
  脚注内部继续包含 `inline code`、*强调文本* 与一小段中文说明，
  用于验证复杂内容不会破坏脚注区域排版。
]。

== 列表系统

=== 无序列表

- 一级项目用于测试普通项目符号。

- 第二个项目包含一段较长文字，用于观察换行后的悬挂缩进效果是否稳定，同时确认列表项之间的垂直间距。
  - 二级项目用于测试嵌套列表。
  - 二级项目继续补充内容，并包含链接
    #link("https://github.com")[GitHub]。
- 第三个项目包含一个短公式 $a^2 + b^2 = c^2$。

=== 有序列表

+ 第一步：读取输入并完成基本校验。

+ 第二步：根据上下文构造文档元素树。

+ 第三步：执行渲染，输出页面内容。

  + 子步骤：记录耗时。

  + 子步骤：校验边界情况。

+ 第四步：写入结果并产生日志。

=== 松散列表

- 这一项包含两个段落。

  第二段用于验证松散列表在你的自定义列表渲染器下，是否依然具备正确的
  缩进、间距和视觉层次。

- 另一项同样拆成两段。

  这类内容在技术笔记、会议纪要和教材中都比较常见，因此值得单独观察。

== 引用与代码

=== 引用块

#quote(block: true, attribution: [排版测试备注])[
  页面装饰、正文字体、行距、标题样式和题注编号必须协同工作，
  否则单个元素即便好看，也无法形成稳定的整页观感。
]

=== 代码块

```typ
#let square(x) = x * x

#for n in range(1, 5) [
  第 #n 项的平方是 #square(n)。
]
```

```python
def normalize_title(raw: str) -> str:
    value = raw.strip()
    if not value:
        raise ValueError("title cannot be empty")
    return value.replace("_", " ").title()
```

== 公式与数学

行内公式用于验证与中文基线的协调，例如
$sum_(i = 1)^n i = n(n + 1) / 2$。

下面给出一个块级公式：

$
  E = m c^2
$ <eq-energy>

再补一个多行公式，检查块间距与对齐：

$
  f(x) & = integral_0^x (2t + 1) dif t \
       & = x^2 + x
$ <eq-integral>

引用公式时应能正常显示，例如 @eq-energy 与 @eq-integral。

== 图形与表格

#figure(
  kind: "image",
  rect(
    width: 100%,
    height: 5cm,
    radius: 6pt,
    fill: rgb("#f2efe8"),
    stroke: (paint: luma(75%), thickness: 0.8pt),
  ),
  caption: [版式占位图，用于测试图片题注、编号与引用。],
) <fig-layout>

正文中引用图片应显示为 @fig-layout。

#figure(
  kind: "table",
  table(
    columns: (1fr, 1fr, 1fr),
    align: center + horizon,
    stroke: (paint: luma(70%), thickness: 0.6pt),
    table.header([指标], [数值], [备注]),
    [页边距], [1.91 cm], [左右一致],
    [正文字号], [12 pt], [衬线字体],
    [代码字号], [10 pt], [等宽字体],
    [段落首缩], [2 em], [全文启用],
  ),
  caption: [模板关键参数示例表。],
) <tbl-metrics>

表格引用测试见 @tbl-metrics。

== 分页与综合内容

为了尽量覆盖更多元素，这里组合一段含有 *粗体*、#strong[粗粗体]、
#underline[下划线]、`inline code`、链接
#link("https://typst.app/universe")[Universe]、脚注#footnote[
  这是综合段落中的补充脚注。
]、_强调_以及公式 $rho = m / V$ 的文本。

下面补一段稍长内容，用于观察分页前后的连贯性。排版系统在跨页时需要同时处理背景纹理、页眉横线、页码
、段落间距和标题前后的留白。如果某个局部规则覆盖顺序不正确，通常会在这类连续正文里暴露出来。

=== 小结性段落

这份测试文档的目的不是展示具体内容，而是尽可能完整地覆盖主题中的主要元素，
从而让你在调整样式后快速回归检查视觉结果与编号行为。


= 第二章测试

$
  f(x) & = integral_0^x (2t + 1) dif t \
       & = x^2 + x \
  "f(x)是一个积分(2t+1)的函数"
$
